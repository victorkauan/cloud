<ul>
  {{#each products}}
    <li>
      <h2>Nome : {{this.name}}</h2>
      <p><strong>Descricao</strong> : {{this.description}}</p>
      <p><strong>Preço</strong> : {{this.price}}</p>
      <p><strong>Categoria</strong> : {{this.tag}}</p>
      <p><strong>ID</strong> : {{this.id}}</p>
      <form action='/admin/produtos/editar/{{this.id}}' method='GET'>
        <input type='submit' value='Editar' />
      </form>
    </li>
  {{/each}}
  <form action='/admin'>
    <input type='submit' value='Voltar ao painel' />
  </form>
</ul>